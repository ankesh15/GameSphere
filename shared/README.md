# GameSphere Shared

Shared types and constants for GameSphere services.

## Usage
- Import types from `shared/src/types.ts`.
- Import constants from `shared/src/constants.ts`.

This folder is intentionally framework-agnostic so it can be used by both
TypeScript services and tooling.
